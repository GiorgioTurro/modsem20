export class Result {
    public uri: string;
    public label: string;
    public comment: string;
    constructor(uri:string, label:string, comment:string){
        this.uri = uri;
        this.label = label;
        this.comment = comment
    }
}
